apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pluralia.fullname" . }}-test-connection"
  labels:
    {{- include "pluralia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pluralia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
